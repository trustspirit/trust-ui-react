---
sidebar_position: 26
title: ActionSheet
---

import { ActionSheet, Button } from 'trust-ui-react';
import { useState } from 'react';
import { ComponentPreview } from '@site/src/components/ComponentPreview';
import { PropsTable } from '@site/src/components/PropsTable';

# ActionSheet

A pre-styled action picker built on top of [BottomSheet](./bottom-sheet.mdx). Renders a vertical list of buttons with optional title and Cancel — modeled after iOS UIActionSheet. Use it when the user needs to **pick one action from a short list** (3-6 items), typically as a "more actions" or "share" picker.

## Basic Usage

export const BasicActionSheet = () => {
  const [open, setOpen] = useState(false);
  return (
    <>
      <Button variant="outline" onClick={() => setOpen(true)}>Show actions</Button>
      <ActionSheet
        open={open}
        onClose={() => setOpen(false)}
        title="Manage post"
        actions={[
          { label: 'Edit', onClick: () => setOpen(false) },
          { label: 'Share', onClick: () => setOpen(false) },
          { label: 'Move to draft', onClick: () => setOpen(false) },
          { label: 'Delete', onClick: () => setOpen(false), destructive: true },
        ]}
        cancelLabel="Cancel"
      />
    </>
  );
};

<ComponentPreview code={`const [open, setOpen] = useState(false);

<ActionSheet
  open={open}
  onClose={() => setOpen(false)}
  title="Manage post"
  actions={[
    { label: 'Edit', onClick: handleEdit },
    { label: 'Share', onClick: handleShare },
    { label: 'Move to draft', onClick: handleDraft },
    { label: 'Delete', onClick: handleDelete, destructive: true },
  ]}
  cancelLabel="Cancel"
/>`}>
  <BasicActionSheet />
</ComponentPreview>

## Destructive actions

Pass `destructive: true` on an action to render its label in danger color + semibold weight. This is the iOS convention for delete / sign-out / unrecoverable operations.

```tsx
{ label: 'Delete project', onClick: confirmDelete, destructive: true }
```

The destructive style is purely visual — it does **not** add a confirmation step. Pair it with a Dialog (or a follow-up ActionSheet) if you want a second prompt before the action fires.

## Without Cancel button

Omit `cancelLabel` to render no Cancel — handy on Android where the system back button serves the same purpose, or when every action is non-destructive.

```tsx
<ActionSheet
  open={open}
  onClose={close}
  actions={[
    { label: 'Photo', onClick: takePhoto },
    { label: 'Video', onClick: takeVideo },
    { label: 'Choose from library', onClick: openLibrary },
  ]}
/>
```

## Disabled actions

Each action can be individually disabled via `action.disabled`:

```tsx
{ label: 'Restore', onClick: handleRestore, disabled: !hasBackup }
```

## Auto-close behavior

ActionSheet does **not** auto-close on action press — your `onClick` handler is responsible for calling `onClose` (or transitioning to another sheet). This is deliberate: it lets you chain confirmation flows without flicker.

```tsx
actions={[
  {
    label: 'Delete',
    destructive: true,
    onClick: async () => {
      await deleteItem();
      setOpen(false);          // close after success
    },
  },
]}
```

## Mobile considerations

- Each action row meets the 44×56px tap target (taller than minimum for thumb comfort).
- Inherits all BottomSheet behavior: `.tui-glass` surface, safe-area awareness, body-scroll lock.
- `showHandle` is forced off and `dismissible` is forced false for ActionSheet — users dismiss via Cancel or backdrop tap, not by dragging.
- For 6+ options or actions with rich content (icons, descriptions), use a plain BottomSheet with your own list instead.

## Props

<PropsTable props={[
  { name: 'open', type: 'boolean', description: 'Whether the sheet is open (controlled)', required: true },
  { name: 'onClose', type: '() => void', description: 'Called when the sheet should close (backdrop tap or cancel)', required: true },
  { name: 'actions', type: 'ActionSheetAction[]', description: '{ label, onClick, destructive?, disabled? } — see below', required: true },
  { name: 'title', type: 'ReactNode', description: 'Optional title shown above the actions' },
  { name: 'cancelLabel', type: 'string', description: 'Label for the Cancel button. If undefined, no Cancel is rendered.' },
]} />

### `ActionSheetAction`

<PropsTable props={[
  { name: 'label', type: 'string', description: 'Visible label for the action', required: true },
  { name: 'onClick', type: '() => void', description: 'Click handler (sheet does not auto-close — call onClose yourself)', required: true },
  { name: 'destructive', type: 'boolean', default: 'false', description: 'Render label in danger color + semibold (iOS destructive style)' },
  { name: 'disabled', type: 'boolean', default: 'false', description: 'Disable this action' },
]} />
