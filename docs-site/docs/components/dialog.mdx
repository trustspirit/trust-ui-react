---
sidebar_position: 14
title: Dialog
---

import { Dialog, Button } from 'trust-ui-react';
import { DialogDemo } from '@site/src/components/DialogDemo';
import { ComponentPreview } from '@site/src/components/ComponentPreview';
import { PropsTable } from '@site/src/components/PropsTable';

# Dialog

A modal dialog overlay for confirmations, forms, or important content. Built on the native `<dialog>` element.

## Preview

Click the buttons below to open dialogs of different sizes.

<ComponentPreview code={`const [open, setOpen] = useState(false);

<Button onClick={() => setOpen(true)}>Open Dialog</Button>
<Dialog open={open} onClose={() => setOpen(false)} size="md">
  <Dialog.Title onClose={() => setOpen(false)}>
    Dialog Title
  </Dialog.Title>
  <Dialog.Content>
    Dialog body content goes here.
  </Dialog.Content>
  <Dialog.Actions>
    <Button variant="ghost" onClick={() => setOpen(false)}>Cancel</Button>
    <Button onClick={() => setOpen(false)}>Confirm</Button>
  </Dialog.Actions>
</Dialog>`}>
  <DialogDemo size="sm" />
  <DialogDemo size="md" />
  <DialogDemo size="lg" />
</ComponentPreview>

## Sizes

The `size` prop controls the width of the dialog panel.

| Size | Description |
|------|-------------|
| `sm` | Small (400px) |
| `md` | Medium (560px) |
| `lg` | Large (720px) |
| `fullscreen` | Full viewport |

## Dialog Props

<PropsTable props={[
  { name: 'open', type: 'boolean', description: 'Whether the dialog is open', required: true },
  { name: 'onClose', type: '() => void', description: 'Callback when the dialog should close', required: true },
  { name: 'size', type: "'sm' | 'md' | 'lg' | 'fullscreen'", default: "'md'", description: 'Size preset' },
  { name: 'closeOnBackdrop', type: 'boolean', default: 'true', description: 'Close when backdrop is clicked' },
  { name: 'closeOnEscape', type: 'boolean', default: 'true', description: 'Close when Escape key is pressed' },
  { name: 'children', type: 'ReactNode', description: 'Dialog content' },
]} />

## Dialog.Title Props

<PropsTable props={[
  { name: 'showClose', type: 'boolean', default: 'true', description: 'Show close button' },
  { name: 'onClose', type: '() => void', description: 'Close handler called when close button is clicked' },
  { name: 'children', type: 'ReactNode', description: 'Title text' },
]} />

## Dialog.Content Props

<PropsTable props={[
  { name: 'children', type: 'ReactNode', description: 'Body content' },
]} />

## Dialog.Actions Props

<PropsTable props={[
  { name: 'children', type: 'ReactNode', description: 'Action buttons' },
]} />
