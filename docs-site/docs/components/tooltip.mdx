---
sidebar_position: 6
title: Tooltip
---

import { Tooltip, Button } from 'trust-ui-react';
import { ComponentPreview } from '@site/src/components/ComponentPreview';
import { PropsTable } from '@site/src/components/PropsTable';

# Tooltip

A popup that displays informative text when hovering or focusing on a trigger element.

## Basic Usage

<ComponentPreview code={`<Tooltip content="Hello from tooltip!">
  <Button>Hover me</Button>
</Tooltip>`}>
  <Tooltip content="Hello from tooltip!">
    <Button>Hover me</Button>
  </Tooltip>
</ComponentPreview>

## Placement

<ComponentPreview code={`<Tooltip content="Top" placement="top">
  <Button variant="outline">Top</Button>
</Tooltip>
<Tooltip content="Bottom" placement="bottom">
  <Button variant="outline">Bottom</Button>
</Tooltip>
<Tooltip content="Left" placement="left">
  <Button variant="outline">Left</Button>
</Tooltip>
<Tooltip content="Right" placement="right">
  <Button variant="outline">Right</Button>
</Tooltip>`}>
  <Tooltip content="Top" placement="top">
    <Button variant="outline">Top</Button>
  </Tooltip>
  <Tooltip content="Bottom" placement="bottom">
    <Button variant="outline">Bottom</Button>
  </Tooltip>
  <Tooltip content="Left" placement="left">
    <Button variant="outline">Left</Button>
  </Tooltip>
  <Tooltip content="Right" placement="right">
    <Button variant="outline">Right</Button>
  </Tooltip>
</ComponentPreview>

## Variants

<ComponentPreview code={`<Tooltip content="Dark tooltip" variant="dark">
  <Button variant="outline">Dark</Button>
</Tooltip>
<Tooltip content="Light tooltip" variant="light">
  <Button variant="outline">Light</Button>
</Tooltip>`}>
  <Tooltip content="Dark tooltip" variant="dark">
    <Button variant="outline">Dark</Button>
  </Tooltip>
  <Tooltip content="Light tooltip" variant="light">
    <Button variant="outline">Light</Button>
  </Tooltip>
</ComponentPreview>

## Mobile behavior

Tooltips have no hover on touch devices, so v2 ships with **four explicit mobile modes**. Tooltip auto-picks the right mode based on what's being wrapped, but you can override with `mobileBehavior`.

| Mode | Interaction | Auto-default for |
|---|---|---|
| `tap` | Single tap toggles; auto-dismisses after `mobileAutoDismiss` ms | Plain text, icons, badges |
| `longpress` | Press and hold 500ms; release dismisses | `<button>`, `<a>` (so tap still fires the action) |
| `inline` | Renders content as helper text below the trigger | `<input>`, `<textarea>`, `<select>` (so focus isn't fought) |
| `hidden` | No mobile rendering | Decorative tooltips |

```tsx
<Tooltip content="Saved automatically" mobileBehavior="longpress">
  <Button>Save</Button>
</Tooltip>

<Tooltip content="Required field" mobileBehavior="inline">
  <input type="email" />
</Tooltip>
```

Pair with `mobileIndicator` to add a subtle dotted underline on the trigger — useful for `tap` mode where users wouldn't otherwise know a tooltip exists.

```tsx
<Tooltip content="Updated 2 minutes ago" mobileIndicator>
  <span>Status</span>
</Tooltip>
```

## Props

<PropsTable props={[
  { name: 'content', type: 'ReactNode', description: 'Content to display inside the tooltip' },
  { name: 'placement', type: "'top' | 'bottom' | 'left' | 'right'", default: "'top'", description: 'Placement relative to the trigger' },
  { name: 'variant', type: "'dark' | 'light'", default: "'dark'", description: 'Visual variant' },
  { name: 'delay', type: 'number', default: '200', description: 'Delay in milliseconds before showing' },
  { name: 'maxWidth', type: 'number', default: '250', description: 'Maximum width in pixels' },
  { name: 'mobileBehavior', type: "'tap' | 'longpress' | 'inline' | 'hidden'", default: 'auto-detected from trigger', description: 'How the tooltip behaves on touch devices' },
  { name: 'mobileAutoDismiss', type: 'number', default: '4000', description: 'Auto-dismiss duration in tap mode (ms). Set 0 to disable.' },
  { name: 'mobileIndicator', type: 'boolean', default: 'false', description: 'Add a dotted-underline visual hint on the trigger' },
  { name: 'mobileMaxWidth', type: 'number', description: 'Override maxWidth on mobile (defaults to maxWidth)' },
  { name: 'children', type: 'ReactElement', description: 'Trigger element' },
  { name: 'className', type: 'string', description: 'Additional CSS class name' },
]} />
